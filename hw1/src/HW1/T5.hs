module HW1.T5
  ( splitOn
  , joinWith
  ) where

import Data.List.NonEmpty (NonEmpty)

splitOn :: Eq a => a -> [a] -> NonEmpty [a]
splitOn = undefined

joinWith :: a -> NonEmpty [a] -> [a]
joinWith = undefined
